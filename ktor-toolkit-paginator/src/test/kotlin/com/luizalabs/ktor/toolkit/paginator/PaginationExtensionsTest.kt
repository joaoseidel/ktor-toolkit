package com.luizalabs.ktor.toolkit.paginator

import io.kotest.core.spec.style.ShouldSpec

class PaginationExtensionsTest :
    ShouldSpec({
        context("PaginationRequest") {
            context("convert to Pagination") {
                should("convert PaginationRequest to Pagination") {
                }
            }
        }
    })
